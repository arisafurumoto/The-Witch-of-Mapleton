# Godot 4+ engine cache and imported binaries (regenerated automatically).
# NOTE: Per-asset *.import files are intentionally NOT ignored — they hold
# import settings and stable resource UIDs and must be committed.
.godot/

# Godot export artifacts
export.cfg
export_presets.cfg

# Imported translations (auto-generated from CSV)
*.translation

# Mono / C# (unused here, kept for safety)
.mono/
data_*/
mono_crash.*.json

# macOS
.DS_Store
